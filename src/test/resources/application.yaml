spring:
  h2:
    console:
      enabled: true
  application:
    name: facemash
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    jdbc-url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
server:
  port: 8081